# AI Customer Message Processor

A minimal AI-based system that processes customer messages to output a category classification, sentiment analysis, and an automated professional response.

## Setup Instructions

1. **Clone/Unzip the project directory**
2. **Install dependencies:**
   ```bash
   pip install -r requirements.txt
